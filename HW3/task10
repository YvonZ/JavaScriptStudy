const arr = [3,2,1]

function reverse(arr) {
    for (let i = arr.length-1; i >= 0; i--) {
        for (let j = i; j >= 0; j--) {
              let temp = arr[i];
              arr[i] = arr[j];
              arr[j] = temp;
        }
    }
    console.log(arr)
    }

reverse(arr);
